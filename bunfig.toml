[install]
# Automatically install peer dependencies
peer = true

# Use exact versions for reproducibility
exact = true

[install.cache]
# Enable global cache
dir = "~/.bun/install/cache"

[run]
# Enable hot reloading for dev scripts
bun = true
